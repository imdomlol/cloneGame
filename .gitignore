node_modules/
dist/
.llmwiki/state.json
.llmwiki/candidates/
__pycache__/
*.pyc
.pytest_cache/

# Compiler outputs — regenerated by `llmwiki compile` + `phase1_sort.py`.
# Track the schema and configs (game-config.json, schemas/, phase1.config.toml),
# not the derived vault/wiki content. Flip these on/off depending on whether you
# want the build artifact in version control.
wiki/concepts/
vault/
build/

# Local secrets
.env
.env.local
