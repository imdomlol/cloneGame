// AUTO-GENERATED stub written by phase2/scaffold.py.
//
// Regenerated on every loop turn by phase2/entrypoint.py once any plugin
// exists under src/. Until then this empty no-op keeps `main.rs` and
// `app_smoke.rs` compiling so the smoke gate can run even at cold start.

use bevy::prelude::*;

/// Empty stub. Replaced after the first plugin lands.
pub fn add_all(app: &mut App) -> &mut App {
    app
}
